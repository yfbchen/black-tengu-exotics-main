import styled from "styled-components";

const Container = styled.div`
  align-items: center;
  display: flex;
  flex-wrap: wrap;
  gap: 32px;
  justify-content: center;
  margin-bottom: 32px;
`;

export const Styled = {
  Container,
};
